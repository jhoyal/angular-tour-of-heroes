# √ Create a Dockerfile that creates a Windows Node Server
# Create a Dockefile that uses the Node Server to Build and angular app
# Create a Dockerfile that serves an angular app from IIS
# Create a Dockerfile that serves an angular app from IIS via HTTPS
# Creata a Dockerfile that serves a .NET Core API
# Consume API from docker container with angular docker container (using Docker Compose)
# Create Repo with all these dockerfiles
